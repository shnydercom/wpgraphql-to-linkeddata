fragment PostPreview on Post {
  title
  preview {
    node {
      excerpt
      featuredImage {
        node {
          altText
          description(format: RAW)
          sourceUrl
          srcSet(size: THUMBNAIL)
        }
      }
    }
  }
  slug
}
